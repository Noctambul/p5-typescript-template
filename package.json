{
  "name": "p5-template",
  "version": "1.0.0",
  "license": "MIT",
  "author": "Noctambul",
  "dependencies": {
    "p5": "^1.3.1",
    "parcel": "^2.0.0"
  },
  "devDependencies": {
    "@types/p5": "^1.3.0"
  },
  "scripts": {
    "start": "parcel index.html",
    "cleanup": "rm -rf dist .cache",
    "build": "parcel build index.html --public-url ./"
  }
}
